import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.d4v8ere.mipeliculasapp',
  appName: 'MiPeliculasApp',
  webDir: 'www'
};

export default config;
